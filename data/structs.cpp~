#include <SDL2/SDL.h>


// TODO: There is a high chance that keycodes can`t be constants for some reason

// Sound control keybinds
struct soundcontrol_t
{
const SDL_Keycode MODIFIER1 = SDLK_LCTRL;
const SDL_Keycode MODIFIER2 = SDLK_RCTRL;

const SDL_Keycode SOUND = SDLK_s;
const SDL_Keycode MUSIC = SDLK_m;
}
soundcontrol;


// Pointer settings
struct pointer_t
{
/*  Sens would have to be implemented the hard way
 *  Take pre move mouse location and compare with post
 *  Then multiply by sens
 * 
*/
const float XSENSITIVITY = 1;
const float YSENSITIVITY = 1;

// Could split acceleration into x and y
const float ACCELERATION = 0;
// Offset till acceleration starts taking effect
const float ACCELERATIONTHRESHOLD = 0;
// Maximum mouse speed
const float ACCELERATIONMAX = 0;
// Linear acceleration or logarithmic
// Consider using float
const bool ACCELERATIONLOGARITHMIC = 0;

} 
mouse;

